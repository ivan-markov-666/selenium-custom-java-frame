## 🔧 ЗАДАЧА #1: Имплементирай Log4j2 Logging

### 📝 Описание на проблема:
В момента целият framework използва `System.out.println()` за логове. Това е проблематично защото:
- ❌ Логовете не се записват във файл за анализ
- ❌ Няма нива на логване (ERROR, WARN, INFO, DEBUG)
- ❌ Няма timestamps за проследяване
- ❌ Не е thread-safe при parallel test execution
- ❌ Невъзможно филтриране и търсене в логове
- ❌ Трудно debugging в production environment

### 🎯 Цел:
Замени всички `System.out.println()` с професионален logging framework (Log4j2) който позволява:
- Записване на логове във файлове
- Различни нива на логване
- Автоматични timestamps
- Thread-safe операции
- Ротация на log файлове
- Лесно филтриране

### 📊 Приоритет:
⭐⭐⭐⭐⭐ (Висок - MUST-HAVE за production-ready код)

### 📦 Засегнати файлове:
- `MainMethods.java` (~30+ места със System.out.println)
- `OtherMethods.java` (~10+ места)
- `WaitTypes.java` (~10+ места)
- `ImportDataFromExcel.java` (~5+ места)
- `BaseTestClass.java` (~5+ места)

### 💡 Бележки:
- Log4j2 dependency вече е добавен в `pom.xml` (редове 94-99)
- Трябва да създадеш конфигурационен файл за Log4j2
- Трябва да определиш кои съобщения са ERROR, кои INFO, кои DEBUG и т.н.

---

## 🔗 ЗАДАЧА #2 Имплементирай Fluent Interface Pattern

### 📝 Описание на проблема:
Методите в `MainMethods` връщат `void`, което прави кода многословен и с много повторения:

```java
mainMethods.navigateURL("https://demoqa.com", titleElement);
mainMethods.fillWithText("John Doe", nameField);
mainMethods.fillWithText("john@test.com", emailField);
mainMethods.clickMethod(submitButton, confirmElement);
```

Всеки ред започва с `mainMethods.` което е повтарящо се и затруднява четенето.

### 🎯 Цел:
Имплементирай Fluent Interface pattern който позволява method chaining (верига от методи):

```java
mainMethods
    .navigateURL("https://demoqa.com", titleElement)
    .fillWithText("John Doe", nameField)
    .fillWithText("john@test.com", emailField)
    .clickMethod(submitButton, confirmElement);
```

### 🎯 Технически изисквания:
- Всеки "action" метод (методи които изпълняват действия, но не връщат данни) трябва да връща `this` вместо `void`
- Методи които връщат данни (`getElement()`, `getElementText()`, `isElementPresent()` и т.н.) НЕ трябва да се променят
- Съществуващите тестове могат да продължат да работят със стария стил (backwards compatible)

### 📊 Приоритет:
⭐⭐⭐ (Среден - Nice to have, подобрява четимостта, но не е критичен)

### 📦 Засегнати файлове:
- `MainMethods.java` (~40 методи за промяна)
- Опционално: Всички тестови файлове ако искаш да използваш новия стил

### ⚠️ Предупреждения:
- Голяма промяна в кода
- Може да счупи тестове ако не си внимателен
- Направи backup преди да започнеш
- Тествай след всяка промяна

---

## 🏗️ ЗАДАЧА #3: Имплементирай Builder Pattern за Test Data

### 📝 Описание на проблема:
Създаването на тестови данни е разпръснато и повтарящо се в тестовете. В много тестове виждаме:

```java
@Test
public void testRegistration() {
    String firstName = faker.name().firstName();
    String lastName = faker.name().lastName();
    String email = faker.internet().safeEmailAddress();
    String phone = otherMethods.randomString("0123456789");
    String gender = "Male";
    // ... още 10 променливи
    
    // Попълваш формата...
}
```

Това води до:
- ❌ Дублиран код във всеки тест
- ❌ Трудно поддържане (ако добавиш ново поле, трябва да обновиш всички тестове)
- ❌ Много локални променливи
- ❌ Не е reusable

### 🎯 Цел:
Имплементирай Builder Pattern за създаване на тестови обекти (User, Address, FormData и т.н.) по гъвкав и reusable начин.

Примерна крайна употреба:
```java
@Test
public void testRegistration() {
    TestUser user = new TestUserBuilder()
        .withRandomFirstName()
        .withRandomLastName()
        .withRandomEmail()
        .withRandomPhone()
        .build();
    
    // Използваш готовия user обект
    mainMethods.fillWithText(user.getFirstName(), page.firstName);
    mainMethods.fillWithText(user.getLastName(), page.lastName);
    // ...
}
```

### 🎯 Технически изисквания:
- Създай модел класове (POJO) за тестови данни (TestUser, TestAddress и т.н.)
- Създай Builder класове които генерират тези обекти
- Builders трябва да поддържат:
  - Random data generation (използвай Faker)
  - Custom data (за специфични случаи)
  - Method chaining (fluent interface)
- Трябва да е лесно разширяемо за нови полета

### 📊 Приоритет:
⭐⭐ (Нисък - Полезно за големи проекти с много тестове, може да е overkill за малки frameworks)

### 📦 Нови файлове за създаване:
- Модел класове (например: `TestUser`, `TestAddress`)
- Builder класове (например: `TestUserBuilder`, `AddressBuilder`)

### 📦 Засегнати тестови файлове:
- Тестове където се генерират много тестови данни (особено A20_* tests)

### 💡 Бележки:
- Започни с един Builder (например TestUserBuilder)
- Тествай че работи преди да правиш други Builders
- Не е нужно да обновиш всички тестове наведнъж

---